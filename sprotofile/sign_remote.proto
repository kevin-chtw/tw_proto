syntax = "proto3";

package sproto;
option go_package = "../sproto";

message SignReq {
  string uid = 1;// 用户ID
  int32 matchid = 2; //比赛ID
  int32 cond_index = 3; //条件索引
}

message SignAck {
  int64 carry_max = 1; //报名时实际可携带分上限
}
