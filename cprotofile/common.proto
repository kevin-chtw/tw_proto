syntax = "proto3";

package cproto;
option go_package = "../cproto";

import "err.proto";

message CommonResponse {
  ErrCode err = 1;
}

message ErrorMessage {
  ErrCode err = 1;
  string msg = 2;
}
